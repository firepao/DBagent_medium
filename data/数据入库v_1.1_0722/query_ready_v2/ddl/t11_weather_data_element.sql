-- 数据集：气象历史数据要素目录
-- 技术表：t11_weather_data_element
-- 描述：当前可用气象历史数据要素、时间粒度、空间粒度与历史时长目录。
-- 覆盖范围：仅为要素目录，不含具体逐时观测值、极端天气事件或预测结果。
-- 行粒度：每行代表一种可用气象历史数据要素。
-- 当前数据画像：共 5 行；全部业务数据属于张家口市全域；画像值来自当前查询数据库。
-- 关联与口径：这是数据要素目录，不是具体观测事实表。
-- 关键词：历史时长、数据要素、时间颗粒度、空间颗粒度、编号
-- 适用问题：可用气象数据要素查询；时间和空间粒度查询
-- 限制：不能回答极端天气影响发电或未来气候预测。
-- 题集问题映射：Q22[supported] 张家口风光资源整体水平怎么样？；Q23[not_supported] 今年以来有哪些极端天气影响发电？
-- 题集已发布口径：无；不得自行补造计算公式或业务阈值。
-- 题集待确认边界：无。
-- 已验证问题示例：无。
-- 字段：id | 别名：主键ID | 类型：INTEGER | 说明：语义类型=integer；来源=源字段；自增主键。 用途=内部追溯或敏感字段，不进入公开查询上下文。 当前库画像：总行数=5，非空=5，空值=0，非空去重值=5；该字段不发布实际取值或代表样例。
-- 字段：source_file | 别名：来源文件 | 类型：TEXT | 说明：语义类型=text；来源=源字段；原始 Excel 文件名。 用途=内部追溯或敏感字段，不进入公开查询上下文。 当前库画像：总行数=5，非空=5，空值=0，非空去重值=1；该字段不发布实际取值或代表样例。
-- 字段：source_sheet | 别名：来源工作表 | 类型：TEXT | 说明：语义类型=text；来源=源字段；原始 Excel 工作表名称。 用途=内部追溯或敏感字段，不进入公开查询上下文。 当前库画像：总行数=5，非空=5，空值=0，非空去重值=1；该字段不发布实际取值或代表样例。
-- 字段：excel_row | 别名：Excel行号 | 类型：INTEGER | 说明：语义类型=integer；来源=源字段；数据在原始 Excel 工作表中的行号。 用途=内部追溯或敏感字段，不进入公开查询上下文。 当前库画像：总行数=5，非空=5，空值=0，非空去重值=5；该字段不发布实际取值或代表样例。
-- 字段：serial_no | 别名：编号 | 类型：INTEGER | 说明：语义类型=integer；来源=源字段；气象历史数据要素表中的“编号”字段，来源表头路径：编号。 用途=标识/追溯，不作为业务指标求和。 当前库画像：总行数=5，非空=5，空值=0，非空去重值=5；当前范围=[1, 5]；当前完整取值="1"、"2"、"3"、"4"、"5"；数值比较和聚合使用本列，不对带单位原文列做字符串计算。
-- 字段：data_element | 别名：数据要素 | 类型：TEXT | 说明：语义类型=text；来源=源字段；气象历史数据要素表中的“数据要素”字段，来源表头路径：数据要素。 用途=低基数分类筛选或分组；只能使用已发布枚举值。 当前库画像：总行数=5，非空=5，空值=0，非空去重值=5；当前完整枚举="太阳辐射（总辐射、直射、散射）"、"气温（10m）"、"相对湿度（10m)"、"风向（10m / 70m / 100m）"、"风速（10m / 70m / 100m）"；筛选时优先使用上述源值精确匹配。
-- 字段：time_granularity | 别名：时间颗粒度 | 类型：TEXT | 说明：语义类型=text；来源=源字段；气象历史数据要素表中的“时间颗粒度”字段，来源表头路径：时间颗粒度。 用途=低基数分类筛选或分组；只能使用已发布枚举值。 当前库画像：总行数=5，非空=5，空值=0，非空去重值=1；当前完整枚举="15 分钟"；筛选时优先使用上述源值精确匹配。
-- 字段：space_granularity | 别名：空间颗粒度 | 类型：TEXT | 说明：语义类型=text；来源=源字段；气象历史数据要素表中的“空间颗粒度”字段，来源表头路径：空间颗粒度。 用途=低基数分类筛选或分组；只能使用已发布枚举值。 当前库画像：总行数=5，非空=5，空值=0，非空去重值=3；当前完整枚举="全市 3*3km"、"全市 3*3km 格点"、"全市3*3km 格点"；筛选时优先使用上述源值精确匹配。
-- 字段：history_duration | 别名：历史时长 | 类型：TEXT | 说明：语义类型=text；来源=源字段；气象历史数据要素表中的“历史时长”字段，来源表头路径：历史时长。 用途=低基数分类筛选或分组；只能使用已发布枚举值。 当前库画像：总行数=5，非空=5，空值=0，非空去重值=1；当前完整枚举="近10年"；筛选时优先使用上述源值精确匹配。
CREATE TABLE "t11_weather_data_element" (
  "id" INTEGER PRIMARY KEY,
  "source_file" TEXT,
  "source_sheet" TEXT,
  "excel_row" INTEGER,
  "serial_no" INTEGER,
  "data_element" TEXT,
  "time_granularity" TEXT,
  "space_granularity" TEXT,
  "history_duration" TEXT
);
